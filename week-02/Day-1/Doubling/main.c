#include <stdio.h>
#include <stdlib.h>
#include "doubling.h"
// create a function called `doubling` that doubles it's input parameter
// again the parameters value should be stored in a .h file

int doubling (number);

int main()
{
    int doubling(number);
    printf("%d", doubling (number));

    return 0;
}

    int doubling(number){
    int result = number * 2;

return result;

}
